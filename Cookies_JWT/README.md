# User authentication and authorization using json web token, with the help of http-only cookies, authToken, refreshToken 

## Environment Variables

`DATABASE_URI`

`ACCESS_TOKEN_SECRET`

`REFRESH_TOKEN_SECRET`

